# The solution can be converted to a single line

main_list = [[int(input()) for i in range(columns)] for j in range(rows)] 

'''
Here, in the above line of code, we are creating the inner list by taking
 elements as the input, i.e., int(input()), and then appending this list 
 as an element to the outer list. This will result in the creation of a 
 2D array.
'''
